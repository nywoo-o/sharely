import 'package:flutter/material.dart';

class BorrowingScreen extends StatelessWidget {
  const BorrowingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Borrowing Screen'),
    );
  }
}